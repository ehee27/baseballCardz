import { useGetAllPlayersQuery } from './playerSlice'

const PlayersList = () => {
  // const { data, isLoading, error } = useGetAllPlayersQuery()
  // console.log(data)
  return <h1>Player List...</h1>
}

export default PlayersList
